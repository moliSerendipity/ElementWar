# 功能：自适应开发工作流与详细路线

- 状态：Closed（2026-08-27 文档维护验证通过）
- 负责人：Codex / 项目维护者
- 维护日期：2026-08-27
- Roadmap：`DOC-001`
- 当前规则：[`Workflow.md`](../Workflow.md)
- 授权：用户批准精简文档、根规则和项目 Skill，并明确不新增 Feature Spec；本次复用此维护记录。

## 当前结果

- `继续下一阶段` 始终路由到路线中的唯一 `Next`；指定功能先核对依赖，旧对话和记忆不替代实时仓库。
- 质量门槛固定，任务组织和独立审查按风险决定；低风险闭环不制造额外任务，高风险边界不为节省 token 被压缩。
- `AGENTS.md` 只保留硬边界，Skill 只保留上下文路由与执行顺序，Workflow 唯一维护流程语义和完成标准，模板只记录获批切片与证据。
- 授权前只在对话中形成契约；Feature Spec 在授权后按适用性创建，用户明确不需要时不新增。
- 新增复杂度必须比较当前最小实现与最低成本稳定接缝；消费者尚未实施和 Roadmap 中存在未来任务都不能单独决定保留或删除。
- 验证矩阵只列出仓库实际存在的 EditMode、PlayMode 和 Windows64 入口，不再示例不存在的统一脚本。

## 边界与证据

- 2026-08-10 初始切片建立四份路线、89 个任务 ID 和唯一 `Next`，依赖无环；该历史结论保留，不在当前规则中复制旧验收表。
- 2026-08-27 核心加载链 `AGENTS + Skill + Workflow` 从 8,145 个非空白字符 / 208 个非空行降至 5,593 字符 / 103 行；连同模板和验证矩阵从 10,694 字符 / 297 行降至 7,386 字符 / 167 行。
- 本次只修改规则和 Markdown；没有新增 Feature Spec，没有修改运行时代码、Unity 序列化资源或全局 Codex `AGENTS.md`。Unity EditMode、PlayMode、Windows64、人工玩法和性能检查均未运行。
- 收口使用相对链接、实际工具路径、唯一 `Next`、空白、scoped diff 和 Git 暂存区检查；当前开发顺序保持 `CMB-030`。
- 回滚单位：根 `AGENTS.md`、项目 Skill/验证矩阵、Workflow/模板、本记录、`DOC-001` 摘要及元素精简 ADR 的规则措辞。
