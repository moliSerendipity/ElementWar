# ElementWar 仓库规则

适用于整个仓库；深层 `AGENTS.md` 可覆盖子树规则。

## 路由与授权

- 知识问答、只读审查、状态报告和明确低风险小改直接处理。
- 用户明确要求“实现 / 修改 / 修复 / 添加 / 继续”时，视为对需求明确的最小直接实现授权；Fast/Standard 不再等待第二次“开始实施”。
- 本仓库内若上层通用 AGENTS 仅因“任务较大 / 多文件”要求先澄清或二次授权，以本文件更具体的 Fast / Standard / Full 路由为准；只有 Full 的未决高影响选择才暂停确认。
- `继续下一阶段`、`开始功能：<ID/名称>`、新增行为、非简单缺陷或重构使用 `$elementwar-feature-workflow`，由 Skill 选择 Fast / Standard / Full。
- 只有状态所有者、跨模块公共契约、asmdef/依赖方向、序列化或 scene/prefab 迁移、Legacy 收敛、发布级性能/兼容性，或多个方案会实质改变玩法/接口/验收时进入 Full。**新功能、跨文件和文件数量本身不是 Full 条件。**
- 仅 Full 读取 `Docs/Workflow.md`；实施中首次出现新的 Full 级选择时再暂停确认。

## 权威来源与边界

- 架构入口：`Docs/Architecture.md`；开发顺序：`Docs/DevelopmentRoadmap.md`。旧对话、记忆和历史计划只作线索。被替代过程默认由 Git 追溯；若存在 `Docs/Archive/**`，默认搜索和读取时排除。
- 用户未提交改动不得覆盖、清理、格式化或吸收。旧架构不是兼容性要求：当前任务不依赖时不修改，确实依赖时允许最小必要修改；迁移/清理按 Full 处理。第三方仅在任务明确针对时修改。
- 不手工编辑/清理 `Library`、`Temp`、`Logs`、`obj`、`.vs`、生成的 `.sln/.csproj`；除非用户要求，不删除、重置、暂存、提交、推送或创建 PR。
- 新增、移动、重命名或删除 `.cs` / Unity 资源时维护 `.meta` / GUID；只修改现有 `.cs` 内容无需检查未变化的 `.meta`。scene/prefab/SO/材质/动画/Input Action/Project Settings 属于序列化边界；只有对象引用、GUID、结构或序列化契约变化时检查受影响引用，普通标量配置调整不做仓库级引用扫描；不批量改 Unity YAML。
- 命令行验证前保存并关闭 Editor；不得用第二个 Unity 进程打开同一工程。

## 实施与交付

- 默认实现当前验收所需的最少状态、类型、分支和抽象；未来可能需求和假设失败路径不构成新增复杂度依据。
- 只处理仓库当前真实存在的生命周期与失败路径；具体复杂度门槛按 Skill 执行。
- 修改 C# 时遵守 `Docs/CodingStandards.md`；**详细 XML、领域规则和逻辑块注释继续保留，不以节省 token 为由删减。**
- 只运行实际存在且与当前切片相关的验证；0 项测试、缺证据、非零退出或旧日志都不是通过，未运行项明确标记。
- 文档优先 Current/Rule、可观察契约和最终证据；原因/历史仅在仍影响未来决策时保留，避免重复权威正文。
