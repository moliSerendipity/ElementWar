# ElementWar 仓库规则

本文件适用于整个仓库；更深目录中的 `AGENTS.md` 可为其子树增加或覆盖规则。

## 大型任务先澄清

新增功能、跨文件/模块修改、架构、公共接口、序列化数据、场景/prefab、技术选型、性能、兼容性或迁移任务必须：

1. 先只读检查相关代码、asmdef、配置、场景、文档、测试和 Git 状态。
2. 自行回答仓库能够确认的事实，只询问会影响设计、范围、行为或验收的问题。
3. 总结目标、非目标、范围、方案、风险和可观察验收标准。
4. 等待用户明确回复“开始实施”“按此执行”或同等授权。
5. 实施中出现新的高影响决策时暂停并重新澄清。

知识问答、只读审查、状态报告和明确的低风险小改动可直接处理。

## 项目事实与边界

- 架构、主线/旧架构路径和已知越界点以 `Docs/Architecture.md` 为唯一维护来源；使用前检查它是否与当前仓库一致。
- 旧架构默认只读，除非用户明确批准迁移或清理。
- 所有既有未提交改动均属于用户；不得清理、覆盖、暂存、提交或格式化无关内容。
- 不修改 `Library`、`Temp`、`Logs`、`obj`、`.vs`、生成的 `.sln/.csproj`。
- 不修改第三方插件源码或二进制，除非任务明确针对该依赖。

## Unity 资源安全

- 保留 `.meta` 和 GUID；新增 Unity 资源时确认对应 `.meta` 被正确生成和跟踪。
- 把 scene、prefab、ScriptableObject、材质、动画控制器、Input Action 和 Project Settings 视为序列化数据迁移。
- 未经明确授权，不批量改写 Unity YAML，也不移动或删除共享资源。
- 修改序列化资源前后检查引用；不要仅凭目录名判断资源是否废弃。
- 不要用第二个 Unity 进程打开同一工程；命令行验证前先保存并关闭 Editor。

## 实施与验证

- 大型任务使用 `$elementwar-feature-workflow`，并从 `Docs/Features/TEMPLATE.md` 建立行为契约。
- 新增或修改 C# 时读取并遵守 `Docs/CodingStandards.md`；只规范本次触及的代码，不批量回填无关旧文件。
- 按最小垂直切片推进，复用现有事实源，不为方便而创建平行状态。
- 遵守 `Docs/Architecture.md` 中的程序集依赖和职责边界。
- 只修改获批范围，不顺手清理旧代码或扩大平台范围。
- 验收与证据等级以 `Docs/Workflow.md` 为准。
- 只运行仓库中实际存在的验证入口；阶段 2 脚本未接入时，不得假定 `Tools/Verify-ElementWar.ps1` 可用。

- 测试数为 0 必须视为失败。
- 未实际运行的测试、构建、手测或性能检查必须明确标记“未运行”。
- 不得用编译成功、旧日志或代码检查替代运行时证据。

## 交付要求

最终报告只陈述有证据的结果，并列出：

- 实际修改的行为和文件。
- 实际执行的命令、测试数和结果。
- XML、日志、Player、截图/录像或 Profiler 证据路径。
- 未运行项目、剩余风险和人工验收步骤。
- Feature Spec/ADR 是否已同步。

除非用户明确要求，不执行清理、重置、删除、暂存、提交、推送或创建 PR。
